function fuga(x){
   let t = Math.random()*4;//Intervalos entre números que podem ser sorteados
   let sort = t.toFixed(0);
   if(sort==1){}
   else{
   x.style.position="absolute";
   x.style.left=Math.random()*800+"px";
   x.style.top=Math.random()*300+"px";
   }}
 function clicar(){
 let alerta =document.getElementById("alerta");
 alerta.addEventListener(alert("VOCÊ CONSEGUIU !"));

   }
